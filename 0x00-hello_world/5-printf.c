#include <stdio.h>

/**
  * main - a c program that uses printf function
  * Return: 0 if succesful
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
